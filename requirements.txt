# ──────────────────────────────────────────────────────────────────
# Requirements — scripts/pdf_generator/
# Entorno conda: pdf_gen
# Generado con: conda run -n pdf_gen pip list --format=freeze
# ──────────────────────────────────────────────────────────────────

# Parsing / renderizado de Markdown
markdown-it-py==4.0.0        # motor de Markdown (generate_pdfs.py, generate_word.py)
mdit-py-plugins==0.5.0       # plugin dollarmath para ecuaciones $...$ y $$...$$

# HTML / XML
beautifulsoup4==4.14.3       # parseo de HTML generado (bs4.BeautifulSoup, Tag, NavigableString)
lxml==6.0.2                  # parser XML/HTML para bs4 y manipulación OOXML directa

# Generación PDF
playwright==1.58.0           # renderizado Chromium headless → PDF (generate_pdfs.py)
PyMuPDF==1.27.2.2            # lectura de PDF para outline/bookmarks y validación de enlaces

# Generación DOCX
python-docx==1.2.0           # creación de documentos Word (.docx) (generate_word.py)
latex2mathml==3.79.0         # conversión LaTeX → MathML → OMML para ecuaciones en DOCX
